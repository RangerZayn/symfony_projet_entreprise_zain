{% extends 'base.html.twig' %}

{% block title %}Tableau de Bord{% endblock %}

{% block page_title %}Tableau de Bord{% endblock %}

{% block page_content %}
    <div class="container mx-auto px-4 py-8">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
            <!-- Carte de bienvenue -->
            <div class="bg-white rounded-lg shadow p-6 col-span-1 md:col-span-3">
                <h2 class="text-2xl font-bold text-gray-800 mb-2">Bienvenue dans le Backoffice!</h2>
                <p class="text-gray-600">
                    Bonjour <span class="font-semibold">{{ app.user.firstname }} {{ app.user.lastname }}</span>, 
                    vous êtes connecté(e) avec le rôle:
                    <span class="inline-block bg-blue-200 text-blue-800 text-sm px-2 py-1 rounded ml-1">
                        {% if is_granted('ROLE_ADMIN') %}
                            ADMIN
                        {% elseif is_granted('ROLE_MANAGER') %}
                            MANAGER
                        {% else %}
                            USER
                        {% endif %}
                    </span>
                </p>
            </div>

            <!-- Users Card (visible to admins) -->
            {% if is_granted('ROLE_ADMIN') %}
                <div class="bg-white rounded-lg shadow p-6 hover:shadow-lg transition">
                    <div class="flex items-center justify-between">
                        <div>
                            <h3 class="text-lg font-semibold text-gray-800">Gestion des Utilisateurs</h3>
                            <p class="text-gray-600 text-sm">Gérer les utilisateurs et leurs rôles</p>
                        </div>
                        <div class="text-4xl text-blue-500">👥</div>
                    </div>
                    <a href="{{ path('user_index') }}" class="mt-4 inline-block bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded transition">
                        Accéder aux Utilisateurs
                    </a>
                </div>
            {% endif %}

            <!-- Products Card -->
            <div class="bg-white rounded-lg shadow p-6 hover:shadow-lg transition">
                <div class="flex items-center justify-between">
                    <div>
                        <h3 class="text-lg font-semibold text-gray-800">Gestion des Produits</h3>
                        <p class="text-gray-600 text-sm">Gérer les produits, prix et stocks</p>
                    </div>
                    <div class="text-4xl">📦</div>
                </div>
                <a href="{{ path('product_index') }}" class="mt-4 inline-block bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded transition">
                    Accéder aux Produits
                </a>
            </div>

            <!-- Clients Card -->
            {% if is_granted('CLIENT_VIEW') %}
                <div class="bg-white rounded-lg shadow p-6 hover:shadow-lg transition">
                    <div class="flex items-center justify-between">
                        <div>
                            <h3 class="text-lg font-semibold text-gray-800">Gestion des Clients</h3>
                            <p class="text-gray-600 text-sm">Gérer les clients et leurs informations</p>
                        </div>
                        <div class="text-4xl">👤</div>
                    </div>
                    <a href="{{ path('client_index') }}" class="mt-4 inline-block bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded transition">
                        Accéder aux Clients
                    </a>
                </div>
            {% endif %}
        </div>

        <!-- Quick Info -->
        <div class="bg-white rounded-lg shadow p-6">
            <h3 class="text-lg font-semibold text-gray-800 mb-4">Informations Rapides</h3>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div>
                    <p class="text-gray-600 text-sm">Utilisateur Actuel</p>
                    <p class="text-lg font-semibold text-gray-800">{{ app.user.email }}</p>
                </div>
                <div>
                    <p class="text-gray-600 text-sm">Rôles Assignés</p>
                    <div class="space-y-1 mt-1">
                        {% for role in app.user.roles %}
                            <span class="inline-block bg-gray-200 text-gray-800 text-xs px-2 py-1 rounded mr-1">
                                {{ role|replace({'ROLE_': ''}) }}
                            </span>
                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
